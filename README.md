# Open-Api-Project
Choosing a cotail by the day of the week

After opening the projects files go to projects directory by using terminal. ( use cd command)
After type "npm init -y"
You will need to install dependencies ( npm install express axios)
run the project ( "node index.js" or "nodemon index.js" if installed)
go to locallhost:3000;

